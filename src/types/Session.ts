interface Session {
    battletag_id: string
    draws: number
    id: string
    losses: number
    name: string
    total_games: number
    total_wins: number
    wins: number
}

export default Session;